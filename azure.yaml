name: techworkshop-l300-ai-agents
infra:
  provider: bicep
  path: src/infra
  module: DeployAzureResources
services:
  app:
    project: ./src
    language: python
    host: containerapp
    docker:
      path: Dockerfile
      context: .
